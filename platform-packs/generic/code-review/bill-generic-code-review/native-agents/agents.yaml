contract_version: "0.1"
agents:
  - { name: bill-generic-code-review-api-contracts, description: "Generic api contracts specialist code reviewer. Runs against Compatibility, validation, serialization, error contracts, and consumer expectations. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-architecture, description: "Generic architecture specialist code reviewer. Runs against Boundary ownership, dependency direction, state lifetime, and change isolation. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-performance, description: "Generic performance specialist code reviewer. Runs against Work amplification, resource lifetime, blocking behavior, and scale-sensitive paths. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-persistence, description: "Generic persistence specialist code reviewer. Runs against Atomicity, consistency, migrations, ownership, and durable state recovery. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-platform-correctness, description: "Generic platform correctness specialist code reviewer. Runs against Runtime semantics, lifecycle invariants, error propagation, and concurrency correctness. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-reliability, description: "Generic reliability specialist code reviewer. Runs against Retries, timeouts, cancellation, recovery, observability, and partial failure. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-security, description: "Generic security specialist code reviewer. Runs against Trust boundaries, authorization, sensitive data, untrusted input, and dangerous side effects. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-testing, description: "Generic testing specialist code reviewer. Runs against Behavioral coverage, failure-path evidence, isolation, determinism, and regression value. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-ui, description: "Generic ui specialist code reviewer. Runs against State ownership, rendering identity, navigation, feedback, and interaction correctness. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
  - { name: bill-generic-code-review-ux-accessibility, description: "Generic ux accessibility specialist code reviewer. Runs against Task completion, semantics, keyboard use, focus, localization, and inclusive interaction. Returns a Risk Register in the F-XXX bullet format.", compose: governed-content }
