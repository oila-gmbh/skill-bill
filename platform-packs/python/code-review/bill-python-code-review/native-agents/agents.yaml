contract_version: "0.1"
agents:
  - name: bill-python-code-review-api-contracts
    description: "Python api contracts specialist code reviewer. Runs against Python API contracts, validation, serialization, and compatibility. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-architecture
    description: "Python architecture specialist code reviewer. Runs against Python package boundaries, import direction, and dependency ownership. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-performance
    description: "Python performance specialist code reviewer. Runs against Python performance risks in hot paths, I/O, memory, imports, and async boundaries. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-persistence
    description: "Python persistence specialist code reviewer. Runs against ORM/raw SQL usage, migrations, transactions, sessions, and data consistency. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-platform-correctness
    description: "Python platform correctness specialist code reviewer. Runs against Python runtime semantics, typing edge cases, resources, concurrency, and time logic. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-reliability
    description: "Python reliability specialist code reviewer. Runs against timeouts, retries, workers, schedulers, external clients, observability, and degradation. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-security
    description: "Python security specialist code reviewer. Runs against dependency supply chain, trust boundaries, secrets, unsafe parsing, subprocesses, and sensitive logs. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-testing
    description: "Python testing specialist code reviewer. Runs against pytest/unittest quality, fixtures, parametrization, integration boundaries, and regression proof. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-ui
    description: "Python ui specialist code reviewer. Runs against Python-rendered UI, templates, admin surfaces, notebooks, and dashboards. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
  - name: bill-python-code-review-ux-accessibility
    description: "Python ux accessibility specialist code reviewer. Runs against server-rendered accessibility, forms, feedback, keyboard flow, and localization-sensitive UX. Returns a Risk Register in the F-XXX bullet format."
    compose: governed-content
