# Content.md Migration Script Retired

The one-off Python migration command `scripts/migrate_to_content_md.py` has
been retired. Current governed skill authoring and repair flows use the
Kotlin-backed `skill-bill render`, `skill-bill fill`, `skill-bill edit`, and
`skill-bill new` commands.

Historical migration notes remain in `docs/migrations/`.

